# Project adapters installed into this repository by install-project.sh - they are generated
# from adapters/, so a committed copy would be a second source of truth for the same rules.
.cursor/
.agents/

# Python bytecode from py_compile / test runs.
__pycache__/
