# HANDOFF — состояние проекта Conductor

Обновлено: 2026-09-26 — добавлена отдельная по проектам память реальных проверок.
Предыдущие исправления ложного успеха, памяти и безопасной отмены сохранены.
Соразмерная проверка не изменилась: обычная надпись допускает просмотр правки без тестов.
Доказательства новой функции и статус публикации — `qa/reports/evidence-2026-09-26.log`;
предыдущая приёмка — `qa/reports/reliability-2026-09-25.md`.
Написано для читателя с нулевым контекстом.

Продолжение текущей сессии, последние проверенные результаты и следующий согласованный
шаг: [заметка от 25 сентября](docs/continuation-2026-09-25.md).
Последнее состояние обновления Graphify, его проверки и оговорки:
[журнал завершения](qa/reports/graphify-2026-09-25/finalization.log).
Полный аудит в `docs/audits/` — исторический снимок **до исправлений**, не текущий список
открытых ошибок. Новые проверки затронутых областей не называются повторным полным аудитом.

## Что это

Conductor — дисциплинарная система для AI-агентов: классификация задачи до работы,
доказательство до слова «готово» и до коммита, самообучение через общую память.
Покрывает четыре среды: Claude Code (ядро + хуки), Cursor, Antigravity, Codex
(текстовые дайджесты правил). Репозиторий — источник правды и одновременно дистрибутив.

## Текущее состояние

- **Память проверок — явный необязательный инструмент.** `runtime/evidence/cli.py`
  сохраняет команду, код завершения, полный вывод и наблюдаемые входы; `check` не запускает
  команду. `MATCH` не означает новый PASS и не доказывает полноту зависимостей. Проекты,
  клоны и worktree разделены, журнал активности не переиспользуется как доказательство.
  Оба установщика доставляют CLI, включая глобальную установку без Claude Code.
  80 тестов на Windows/Linux, установка на трёх языках и отрицательный контроль выполнены.
  В малой агентной пробе ошибок принятия нет, но сокращения запусков не измерено:
  изменение отпечатка окружения потребовало повторной проверки. Подробности и ограничения
  находятся в трёх README и журнале приёмки, не в новых обязательных правилах.
- Установщик дополнительных инструментов отличает подтверждённое состояние superpowers
  от ошибки или непонятного списка. Один успешный снимок и точная привязка статуса к записи;
  неизвестность даёт явный FAIL, но не отменяет установку Conductor. 14 изолированных
  сценариев: `qa/companions-test.py`; первичные журналы: `qa/reports/companions-2026-09-25/`.
- **Хуки и установщики runtime — на Bash.** Отдельный PowerShell-скрипт
  `tools/schedule-memory-backup.ps1` восстанавливает расписание Windows только по явному запуску.
  `python3` нужен установщикам (правка JSON-конфигов других инструментов) и журналу
  прогонов тестов в рантайме — без python журнал молча не пишет ни строки (это его
  осознанный fail-open); остальные хуки — чистый bash.
- Пользовательские `graphify` / `@RTK.md` в глобальном `CLAUDE.md` сохраняются установкой.
  Состояние конкретной машины проверяется командой `bash tools/doctor.sh` — не по памяти
  этого файла: установка могла быть снесена после любой его правки.
- **Маркерный гейт снят окончательно** во всех репозиториях, где стоял; вычистка чужих
  машин — `tools/sweep-git-gate.sh` (см. `uninstall.sh --sweep-roots`).
- **Память перестроена:** накопленные уроки разложены по файлам с индексом
  (`tools/migrate-lessons.sh`); индекс пересобирается из стора при каждом прогоне.
  Установщик доставляет этот инструмент в `memory/`: обслуживание возможно без исходников.
  Свежие строки показываются первыми; длинные записи не вытесняют адреса журнала и индекса.
  Неполные даты уходят в конец индекса, архивы двух разборов в одну секунду не перезаписываются.
  Восстановление полного приватного репозитория и расписания: `docs/memory-recovery.md`.
- **Отмена — только своих изменений.** Точные снимки до задачи учитывают dirty/untracked;
  перед отменой проверяется ожидаемое состояние. Проверка без исправления идёт в отдельной
  копии с сохранением текущего теста. Поздние чужие изменения не перезаписываются.
- **Контур доверия проверяется по структуре и поведению.** `settings-json.py` удаляет
  только собственные leaf-команды внутри общего hook-entry и сохраняет чужих соседей;
  `audit-hooks` проверяет точную форму регистрации, а doctor запускает smoke только после
  успешного аудита. CI гоняет отдельные sandbox/self-test для doctor, журнала и бенча.

## Архитектура

| Слой | Файлы | Кому |
|---|---|---|
| Ядро | `runtime/core.md` (sentinel CONDUCTOR-CORE-v1-7f3a) | Claude Code |
| Плейбуки | `runtime/playbooks/` (debugging, implementing, verification, investigating, orchestration, skeptic, distill, methods) | Claude Code |
| Контракт субагента | `runtime/subagent-contract.md` (правила исполнителя) | субагенты |
| Хуки | `runtime/hooks/*.sh` + общий `payload.sh`; отдельно `test-run-journal.sh` — пассивное наблюдение за прогонами тестов, ничего не блокирует | Claude Code |
| Дайджест | `adapters/core-body.md` — **единственный источник**; `tools/build-digests.sh` собирает из него файлы Cursor и Antigravity | Cursor, Antigravity, Codex |
| Память | `~/.claude/conductor/lessons.md` (входящее) + `lessons/` с `INDEX.md` (разобранное) | все |
| Доказательства проверок | `runtime/evidence/`; данные отдельно от уроков и Git, по проектам | все агенты с Python |

Установка: `install.sh` (Claude Code; язык ответов — меню при каждом запуске с
сохранённым выбором по умолчанию, `--language <имя>` пропускает вопрос),
`install-global.sh` (Cursor/AG/Codex + пересборка глобального
CLAUDE.md, язык спрашивает так же), `install-project.sh
--repo <путь>` (проектные адаптеры; подставляет сохранённый язык, `--language` —
переопределение только для этого проекта, машинный выбор не трогает), `uninstall.sh`
(симметричное удаление, есть `--dry-run`). Выбор языка хранится в
`~/.claude/conductor/reply-language`; повторные запуски установщиков его не сбрасывают.
Шагом [4/5] `install.sh` вызывает новый корневой `install-companions.sh`: superpowers
(маркетплейс плагинов), rtk (cargo), graphify (uv/pip) — по умолчанию ставятся все три.
Политика по superpowers перевёрнута: раньше плагин отключался, `--keep-superpowers` —
теперь принимаемый no-op, отказ — `--no-superpowers`, весь шаг — `--skip-companions`
(его передаёт песочница CI). Провал спутника громкий (SKIP/FAIL с причиной), установку
не валит; шаг идемпотентен, `uninstall.sh` спутников не трогает.

## Ключевые решения волны v2.0

- **Правила приведены к документации Anthropic** по текущему поколению reasoning-моделей:
  - убрана избыточная верификация как инструкция (модели перепроверяют себя сами);
    скептик больше не обязателен на каждом T3 — он для работы, которую ты не видел;
  - роли разведены: ОРКЕСТРАТОР (главная сессия) делегирует крупное и независимое,
    не делегирует мелочь, не проверяет субагентом свою же работу; ИСПОЛНИТЕЛЬ (контракт
    субагента) не делегирует дальше, применяет инструкции ровно в заданной области,
    при поиске дефектов отдаёт всё найденное без самофильтрации;
  - добавлен раздел про длинные автономные прогоны: сверка заявлений с результатами
    инструментов, запрет заканчивать ход обещанием, запрет сворачиваться из-за бюджета
    контекста, пауза только для необратимого/смены объёма/ввода пользователя;
  - effort привязан к тирам (T1 low/medium, T2 high, T3 xhigh);
  - ограничение объёма («не рефактори вокруг фикса») — в `implementing.md` и дайджестах;
  - стиль ответа (сначала итог, читаемость важнее краткости, длина письменных
    артефактов) — в `deploy/global-CLAUDE.md`, у которого нет лимита размера.
- **Один источник дайджестов.** Файлы Cursor и Antigravity были байт-в-байт одинаковыми
  и синхронизировались руками — постоянный генератор расхождений. Теперь тело общее,
  заголовки свои, линтер падает на устаревшем сгенерированном файле.
- **Линтер усилен двумя правилами из документации:** запрет формулировок, требующих
  пересказать внутренние рассуждения (у текущего поколения это классификатор отказа с
  откатом на предыдущую модель), и запрет инструкций «перепроверь себя». Плюс проверка синтаксиса всех
  shell-скриптов. Каждое правило проверено намеренным нарушением.
- **Память двухступенчатая.** Раньше журнал был одним файлом, а впрыскивались только
  10 верхних строк: 142 урока лежали на диске и были невидимы. Теперь входящее
  (дёшево записать) отделено от разобранного (индекс из однострочников, читается
  по необходимости), и рост памяти больше не означает её потерю.
- **Пассивный журнал наблюдений** (`runtime/hooks/test-run-journal.sh`, события `PostToolUse`
  и `PostToolUseFailure`, матчер `Bash`) пишет в `~/.claude/conductor/test-runs.log`.
  Учитываются распознанные простые команды и результат события; упоминания тестов,
  составные команды, неясные обёртки и режимы без запуска исключаются. **Ничего не блокирует.**
  Восьмое поле `v2` отделяет новый формат: старые строки сохранены, но не дают оснований
  для качественных выводов. `tools/journal-report.sh` отдельно показывает историю и мусор.
  Это не защищённое доказательство: full/partial — классификация команды, не гарантия
  реального покрытия; hash не охватывает untracked-файлы. Нужны вывод тестов и проверка
  применимости. Незнакомые команды пропускаются; журнал даёт неполную картину запусков.
  Зачем: `qa/reports/baseline.md:65` требует решать вопрос об урезании правил по данным
  живой работы, а ловушки этот класс задач воспроизвести не могут — журнал их и даёт.
- **Условие про файл ценностей записано в оба README.** Контрольная группа показала
  поведение без Conductor, но с глобальным `CLAUDE.md`; значит удаление этого файла тихо
  снимает дисциплину целиком. Теперь это условие работоспособности, а не примечание.
- **Разбор правил по корзинам проведён** (дублирует / спорит / добавляет / не проверено).
  Удаление НЕ выполнялось сознательно: тот же `baseline.md:65` постановил решать по живым
  данным. Разбор станет входными данными, когда журнал накопит их.
- **Волна ремонта по внешнему ревью (2026-08-15).** Память: индекс уроков пересобирается
  из стора (повторная дистилляция больше его не стирает), инжект берёт новейшие строки и
  режет блок целыми строками (байтовый `cut` ломал UTF-8 кириллицы), инбокс ротируется до
  чтения (гонка параллельной записи закрыта), строки без `|` не файлятся мусором, DISTILL
  DUE эскалируется в каждосообщенческий хук. Инструменты: sentinel в `settings-json.py`
  заякорен на границе слова (semiconductor-* больше не матчится) и покрыт тестами
  (`qa/settings-json-test.py`, 12 шт.), у линта появился негативный самотест
  (`qa/lint-selftest.sh`, 13 кейсов), хелпы всех скриптов извлекаются по маркеру `set`,
  `install-global.sh` громко предупреждает при перезаписи чужого AGENTS.md, журнал видит
  прямые runner-ы как partial. Новое: `tools/doctor.sh` (здоровье установки; родился из
  машины, где хуки тихо не были зарегистрированы) и `tools/journal-report.sh` (чтение
  журнала + вердикт достаточности). Правила: пороги fan-out при исследовании подняты до
  >20 файлов / >1500 строк и углы поиска больше не триггер (противоречили SIZE FLOOR),
  немутирующим задачам дан вход в тиры, лок/генерат исключён из RE-TIER по LOC, ложный
  маркер озвучивается один раз за сессию, CONTROL GROUP сужен до артефактов
  дисциплинарного слоя, cross-model верификатор берётся из
  `~/.claude/conductor/crossmodel.conf` вместо зашитого CLI, отчёты субагентов — вне
  рабочего дерева. Дистрибутив: CI (GitHub Actions: lint + самотесты на ubuntu и
  windows), LICENSE (MIT), `CHANGELOG.md`, англоязычный `README.en.md`.
- **Язык — один токен, корпус — английский (2026-08-15).** `deploy/global-CLAUDE.md`
  переведён на английский: модель рассуждает на языке своих инструкций, и русский корпус
  утягивал видимые рассуждения в русский даже при выборе AZ/EN (при этом старый
  `install-global.sh` подменял лишь одну фразу, а повторный `install.sh` молча возвращал
  русский). Язык ответов — единственная подставляемая фраза «Answer in Russian»
  (общий код — `tools/reply-language.sh`); выбор сохраняется в
  `~/.claude/conductor/reply-language`, его подставляют все три установщика, включая
  `install-project.sh`. Рассуждения (видимый thinking) идут на выбранном языке ответов —
  решение пользователя от 2026-08-15 (первая редакция фикса ставила «всегда на
  английском»); задаётся явной строкой правил, не подстановкой. Линт охраняет токен,
  правило о языке рассуждений и отсутствие кириллицы в корпусе (байтовая проверка,
  не зависит от локали); каждое правило проверено намеренным нарушением.

## Бенчмарк удалён (2026-07-28). Авторизации и ключей в проекте нет

**Conductor не требует авторизации.** Ключи нужны были только измерительному прибору, а не
системе: сломанная авторизация никогда не мешала Conductor работать. Прибор удалён целиком —
`qa/run-benchmark.sh`, сценарии, пять ловушек-фикстур, изолированные каталоги настроек и
стенограммы прогонов. Ключей в репозитории нет и не будет.

Что прибор успел доказать до удаления (единственный состоявшийся прогон, сценарий `verify`):

- **Плечо с Conductor доказано сквозным прогоном.** Сентинел `CONDUCTOR-CORE-v1-7f3a` в
  стенограмме дважды, ноль записей `hook_non_blocking_error`, и — главное — строка
  `Conductor: debug | T1 | core only` в ответе агента. Последнее важнее первого: сентинел
  показывает, что текст доставлен, а объявление типа и уровня — что правила **сработали**.
- **Контрольное плечо не измерялось.** Отказ аутентификации. Причина доказана контрольными
  суммами: прогон обновил OAuth-токен, обновление отзывает прежний, и вторая копия с тем же
  токеном стала недействительной. Два плеча не могут работать от одной скопированной пары
  ключей — кто первым обновит, тот отзовёт у остальных.

Почему прибор не чинили дальше, а убрали: `qa/reports/baseline.md:62-63` прямо говорит, что
эти ловушки не воспроизводят длинные сессии, ради которых правила и держат. Чинить прибор,
заведомо не покрывающий нужный класс, и платить за это выбитой рабочей сессией владельца при
каждом замере — не туда. Данные живой работы даёт журнал прогонов тестов, и он копит их сам.

**Что сохранено намеренно.** Отчёты `qa/reports/` — это измерения, а не прибор: на них
держится разбор правил по корзинам, и README с этим файлом ссылаются на их строки. Там же
лежит `qa/reports/baseline-values-file.md` — те самые 56 строк файла ценностей, с которыми
контрольная группа прошла 13 испытаний из 13; это вещественное доказательство к условию в
README, а не фикстура. Содержимое не менялось, только путь.

Возврат к разбору правил по корзинам — по данным журнала, как предписывает `baseline.md:65`.

## Деплой-цикл (незыблемо)

Правки ТОЛЬКО в `runtime/` и `adapters/core-body.md` → `bash tools/build-digests.sh` →
`bash qa/lint.sh` (он же печатает занятость бюджетов) → установщики → проверка живых
копий grep'ом, не верой.

Бюджеты: payload ядра ≤9500 экранированных символов (харнесс молча режет на 10000 —
**замерь ДО правки ядра**), контракт ≤3000 (в паре с SOFT_CAP хука subagent-start.sh),
плейбуки ≤6000 (implementing ≤6500, orchestration ≤7000, verification ≤5000), probes ≤3600,
дайджесты ≤12000 (Antigravity молча обрезает).

## Незакрытые хвосты

1. **Дистилляция 152 уроков в правила не проводилась.** Механика готова и уроки
   доступны (индекс + файлы), но тематического обобщения в плейбуки не было. Это
   оптимизация, а не починка: невидимости больше нет. Процедура — `playbooks/distill.md`.
2. **Cursor global rule** — ручной шаг: вставить тело
   `~/.claude/conductor/adapters/cursor/conductor-core.mdc` в Cursor Settings → Rules.
   Проектные правила ставятся `install-project.sh`.
3. **A/B-замер перезапущен 2026-08-15 на поставляемом английском корпусе** —
   `qa/reports/ab-report-v2.md`: 27/27 в обоих плечах при n=5 (модель прогона
   зафиксирована в самом отчёте),
   инструмент воскрешён как `qa/bench/` (оба плеча на живой авторизации). Историческое
   плечо conductor измерено в режиме core-only. Текущий runner исключает live user settings
   в обоих плечах и выдаёт каждому invocation отдельные workspace и namespace артефактов;
   плечо Conductor рендерит собственный runtime/settings. Соседний JSON-manifest хранит
   commit, digest фактически скопированного runtime, module base, sentinel и artifact identity.
   Открытыми остались повтор полного A/B с плейбуками, redesign S4 и длинные сессии.
4. **Журнал не даёт автоматического разрешения урезать проверки.** Volume gate:
   ≥200 parseable строк / ≥3 репозитория / ≥21 день. Quality gate применяет те же пороги
   только к относимым `PASS`/`FAIL` и требует оба исхода. Лишь два зелёных гейта дают
   `READY_FOR_CONTROLLED_EXPERIMENT`; удаление правил требует отдельного A/B на тех же
   задачах. Уточнение соразмерности от 2026-09-25 разрешено пользователем отдельно;
   основание — противоречия действующих правил и короткие пробы выбора проверок,
   а не готовность журнала. Это не замер скорости или надёжности на реальных проектах.
5. Версии: тег `v2.1.0` + `CHANGELOG.md` появились 2026-08-15; до этого тегов не было.

## Чего НЕ делать

- Не добавлять фичи без запроса или урока из памяти: система растёт от настоящих провалов.
- Не возвращать механические замки, которые ничего не добавляют к уже выполняемой работе.
  Маркерный гейт снят дважды — сначала в правилах, теперь физически во всех репозиториях:
  агенты и без него выполняли доказательные прогоны, а замок требовал сверх них отдельный
  файл. Проверка была настоящей, лишним был именно замок.
- Не править сгенерированные дайджесты руками — правится `adapters/core-body.md`.
- Еженедельный самотест — отклонён пользователем дважды. Не предлагать.
- `qa/home-*/.credentials.json` — секреты, не коммитить. Глобальный `CLAUDE.md`
  пользователя не удалять никогда (в нём его `graphify` и `@RTK.md`).
